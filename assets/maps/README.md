Coloque aqui o mapa pronto de Kanto com o nome `kanto.png`.

Quando esse arquivo existir, o comando `.mapa` usa a imagem como base e desenha
apenas o marcador de localizacao por cima. Se o arquivo nao existir, o bot usa o
mapa gerado em codigo como fallback.
